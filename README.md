# 物料分类 Demo

Streamlit demo for material ABCXYZ, shelf-life, and MTO/MTS classification.

## Run locally

Install dependencies:

    pip install -r requirements.txt

Start the app:

    streamlit run inventory_demo_app.py

The app only reads Excel files uploaded through the web UI.
